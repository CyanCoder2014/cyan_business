# Phase 5 reference intent

Use these images for the definition catalog, structured field editor, selected
field inspector, nested object/list presentation, data grid, filters, selection,
and record detail drawer/sheet.

The implementation must be definition-driven rather than product-specific.
