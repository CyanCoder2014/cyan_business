import{NextResponse}from"next/server";import{proxyTo}from"@/lib/bff-proxy";const bases:Record<string,string>={auth:process.env.SSO_AUTH_SERVICE_BASE_URL??"http://localhost:9001",users:process.env.SSO_USER_SERVICE_BASE_URL??"http://localhost:9002",captcha:process.env.SSO_CAPTCHA_SERVICE_BASE_URL??"http://localhost:9003",otp:process.env.SSO_OTP_SERVICE_BASE_URL??"http://localhost:9004",sessions:process.env.SSO_SESSION_SERVICE_BASE_URL??"http://localhost:9005",fido:process.env.SSO_FIDO_SERVICE_BASE_URL??"http://localhost:9006"};type C={params:{path:string[]}};async function proxy(r:Request,c:C){const base=bases[c.params.path[0]];if(!base)return NextResponse.json({errorCode:"UNSUPPORTED_SSO_SERVICE",message:"Unsupported SSO service",retryable:false},{status:404});const u=new URL(r.url);return proxyTo(r,`${base}/api/sso/${c.params.path.map(encodeURIComponent).join("/")}${u.search}`)}export const GET=proxy;export const POST=proxy;export const PUT=proxy;export const PATCH=proxy;export const DELETE=proxy;
