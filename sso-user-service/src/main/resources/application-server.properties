# Server deployment profile for Kubernetes service discovery and Envoy Gateway ingress.
eureka.client.enabled=false
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false
spring.datasource.url=${SSO_USER_SERVICE_DATASOURCE_URL:jdbc:postgresql://postgres:5432/sso_user_service}
spring.datasource.driverClassName=org.postgresql.Driver
spring.datasource.username=${SSO_USER_SERVICE_DATASOURCE_USERNAME:${SPRING_DATASOURCE_USERNAME:postgres}}
spring.datasource.password=${SSO_USER_SERVICE_DATASOURCE_PASSWORD:${SPRING_DATASOURCE_PASSWORD:postgres}}
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${JWKS_URI:http://sso-auth-service:9001/.well-known/jwks.json}
service.internal.username=${SSO_USER_SERVICE_INTERNAL_USERNAME:sso_user_internal}
service.internal.password=${SSO_USER_SERVICE_INTERNAL_PASSWORD:sso_user_secret}
