"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {PanelShell} from "@/components/panel-shell";import {usePanel} from "@/components/panel-provider";import {useScopeAccess} from "@/components/scope-access-provider";import {AsyncButton,Dialog,EmptyState,ErrorState,Field,Skeleton,StatusBadge} from "@/components/ui/primitives";import {createSite,listSites,type SiteSummary} from "@/lib/site-admin-api";
export default function Sites(){const {locale}=usePanel();const {tenantKey,queryVersion}=useScopeAccess();const [items,setItems]=useState<SiteSummary[]>([]),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null),[open,setOpen]=useState(false),[pending,setPending]=useState(false),[name,setName]=useState(""),[key,setKey]=useState("");const load=()=>{if(!tenantKey){setLoading(false);return Promise.resolve()}setLoading(true);setError(null);return listSites(tenantKey).then(setItems).catch(e=>setError(e instanceof Error?e.message:String(e))).finally(()=>setLoading(false))};useEffect(()=>{void load()},[tenantKey,queryVersion]);const save=async()=>{if(!tenantKey||pending)return;setPending(true);try{await createSite(tenantKey,{name,siteKey:key||undefined});setOpen(false);setName("");setKey("");await load()}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setPending(false)}};return <PanelShell activeKey="sites" title="Sites" titleFa="سایت‌ها" subtitle="Manage real tenant sites and open their publishing workspace." subtitleFa="سایت‌های واقعی فضای کار را مدیریت و محیط انتشار آن‌ها را باز کنید."><div className="page-action-bar"><StatusBadge tone="info">{items.length} {locale==="fa"?"سایت":"sites"}</StatusBadge><button className="primary-pill" disabled={!tenantKey} onClick={()=>setOpen(true)}>{locale==="fa"?"سایت جدید":"New site"}</button></div>{loading?<Skeleton height={340}/>:error?<ErrorState title="Sites unavailable" description={error} retry={load}/>:items.length?<div className="phase9-card-grid">{items.map(s=><Link className="panel-card phase9-resource-card" key={s.siteKey} href={`/sites/${encodeURIComponent(s.siteKey)}/builder`}><div className="phase9-provider-icon">▣</div><div><p className="page-kicker">{s.siteKey}</p><h2>{s.name}</h2><p>{new Date(s.createdAt).toLocaleDateString(locale)}</p></div><StatusBadge tone="success">{s.status}</StatusBadge></Link>)}</div>:<EmptyState title={locale==="fa"?"سایتی نیست":"No sites"} description={locale==="fa"?"اولین سایت این فضای کار را ایجاد کنید.":"Create the first site in this workspace."}/>}<Dialog open={open} title={locale==="fa"?"سایت جدید":"New site"} onClose={()=>!pending&&setOpen(false)}><div className="phase9-form"><Field label={locale==="fa"?"نام سایت":"Site name"} value={name} onChange={e=>{setName(e.target.value);if(!key)setKey(e.target.value.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,""))}}/><Field label={locale==="fa"?"کلید سایت":"Site key"} value={key} onChange={e=>setKey(e.target.value.toLowerCase().replace(/[^a-z0-9-]/g,""))}/>{error?<p className="field-error">{error}</p>:null}<div className="dialog-actions"><button className="secondary-pill" disabled={pending} onClick={()=>setOpen(false)}>{locale==="fa"?"لغو":"Cancel"}</button><AsyncButton pending={pending} disabled={name.trim().length<2||key.length<2} onClick={save}>{locale==="fa"?"ایجاد":"Create"}</AsyncButton></div></div></Dialog></PanelShell>}
