"use client";import {useEffect,useState} from "react";import {PanelShell} from "@/components/panel-shell";import {BpmDesigner} from "@/components/bpm/bpm-designer";import {ErrorState,Skeleton} from "@/components/ui/primitives";import {getFlow,type DynamicFlowDefinition} from "@/lib/bpm-api";export default function BpmDetail({params}:{params:{flowKey:string}}){const [flow,setFlow]=useState<DynamicFlowDefinition|null>(null);const [error,setError]=useState<string|null>(null);const load=()=>{setError(null);void getFlow(params.flowKey,{}).then(setFlow).catch(e=>setError(String(e)))};useEffect(()=>{load()},[params.flowKey]);return <PanelShell activeKey="flows" title="Process designer" titleFa="طراح فرایند" subtitle={params.flowKey} subtitleFa={params.flowKey}>{error?<ErrorState title="Process unavailable" description={error} retry={load}/>:flow?<BpmDesigner initial={flow}/>:<Skeleton height={680}/>}</PanelShell>}
