"use client";
import Link from "next/link";import {useEffect,useState} from "react";import {PanelShell} from "@/components/panel-shell";import {usePanel} from "@/components/panel-provider";import {useScopeAccess} from "@/components/scope-access-provider";import {EmptyState,ErrorState,Skeleton,StatusBadge} from "@/components/ui/primitives";import {createDefinitionFromTemplate,dynamicServices,listDefinitions,listTemplates} from "@/lib/dynamic-api";import type {DynamicEntityDefinition,DynamicEntityTemplate,DynamicServiceKey} from "@/lib/types";
export default function Definitions(){const{locale}=usePanel();const{tenantKey,siteKey,queryVersion}=useScopeAccess();const[service,setService]=useState<DynamicServiceKey>("content-service");const[definitions,setDefinitions]=useState<DynamicEntityDefinition[]>([]);const[templates,setTemplates]=useState<DynamicEntityTemplate[]>([]);const[loading,setLoading]=useState(true);const[error,setError]=useState<string|null>(null);const[search,setSearch]=useState("");const[creating,setCreating]=useState<string|null>(null);const scope={tenantKey:tenantKey||undefined,siteKey:siteKey||undefined};async function load(){if(!tenantKey)return;setLoading(true);setError(null);try{const[d,t]=await Promise.all([listDefinitions(service,scope),listTemplates(service)]);setDefinitions(d);setTemplates(t);}catch(e){setError(e instanceof Error?e.message:"Definitions unavailable");}finally{setLoading(false);}}useEffect(()=>{void load();},[service,tenantKey,siteKey,queryVersion]);const visible=definitions.filter(x=>`${x.entityKey} ${x.title||""}`.toLowerCase().includes(search.toLowerCase()));async function fromTemplate(t:DynamicEntityTemplate){const key=window.prompt(locale==="fa"?"کلید یکتای موجودیت":"Unique entity key",t.templateKey);if(!key)return;setCreating(t.templateKey);try{const value=await createDefinitionFromTemplate(service,t.templateKey,key,scope);setDefinitions(v=>[value,...v.filter(x=>x.entityKey!==value.entityKey)]);}catch(e){setError(e instanceof Error?e.message:"Creation failed");}finally{setCreating(null);}}return <PanelShell activeKey="maker" title="Definitions & Forms" titleFa="تعریف‌ها و فرم‌ها" subtitle="Browse service-owned schemas and templates." subtitleFa="ساختارها و قالب‌های متعلق به هر سرویس را مدیریت کنید."><div className="definition-catalog"><aside><label>Service<select value={service} onChange={e=>setService(e.target.value as DynamicServiceKey)}>{dynamicServices.map(s=><option key={s}>{s}</option>)}</select></label><input value={search} onChange={e=>setSearch(e.target.value)} placeholder={locale==="fa"?"جستجوی تعریف":"Search definitions"}/></aside><section><header><h2>{service}</h2><span>{definitions.length} definitions</span></header>{loading?<Skeleton height={240}/>:error?<ErrorState title="Definitions unavailable" description={error} retry={load}/>:visible.length?<div className="definition-list">{visible.map(x=><Link key={x.entityKey} href={`/definitions/${service}/${encodeURIComponent(x.entityKey)}`}><div><strong>{x.title||x.entityKey}</strong><span>{x.entityKey}</span></div><StatusBadge tone={x.active?"success":"neutral"}>{x.active?"Active":"Inactive"}</StatusBadge><span>r{x.revision??0}</span></Link>)}</div>:<EmptyState title={locale==="fa"?"تعریفی نیست":"No definitions"} description={locale==="fa"?"این سرویس در محدوده فعال تعریفی ندارد.":"This service returned no scoped definitions."}/>}</section><aside><h2>{locale==="fa"?"قالب‌ها":"Templates"}</h2>{templates.map(t=><article key={t.templateKey}><strong>{t.title||t.templateKey}</strong><p>{t.description}</p><button className="secondary-pill" disabled={creating===t.templateKey} onClick={()=>fromTemplate(t)}>{locale==="fa"?"ساخت از قالب":"Create from template"}</button></article>)}</aside></div></PanelShell>}
