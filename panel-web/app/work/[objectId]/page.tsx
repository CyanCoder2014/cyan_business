"use client";
import {useEffect,useMemo,useState} from "react";import {PanelShell} from "@/components/panel-shell";import {AsyncButton,CodeViewer,ErrorState,Skeleton,StatusBadge} from "@/components/ui/primitives";import {addAttachment,addComment,assignManagedObject,getActiveManagedObjectForm,getManagedObject,listAttachments,listComments,listTransitionOptions,setManagedObjectLock,submitManagedObjectForm,transitionManagedObject,type ManagedObject,type ManagedObjectActiveFormResponse,type ManagedObjectAttachment,type ManagedObjectComment,type TransitionOptionResponse} from "@/lib/bpm-api";import {prepareMediaUpload,uploadMediaBytes} from "@/lib/media-api";import {useScopeAccess} from "@/components/scope-access-provider";import {usePanel} from "@/components/panel-provider";
type Field={type?:string;required?:boolean;label?:string};
export default function WorkItem({params}:{params:{objectId:string}}){const {locale}=usePanel();const {tenantKey,siteKey}=useScopeAccess();const scope={tenantKey:tenantKey??undefined,siteKey:siteKey??undefined};const [item,setItem]=useState<ManagedObject|null>(null);const [form,setForm]=useState<ManagedObjectActiveFormResponse|null>(null);const [options,setOptions]=useState<TransitionOptionResponse[]>([]);const [comments,setComments]=useState<ManagedObjectComment[]>([]);const [attachments,setAttachments]=useState<ManagedObjectAttachment[]>([]);const [values,setValues]=useState<Record<string,unknown>>({});const [comment,setComment]=useState("");const [assignee,setAssignee]=useState("");const [pending,setPending]=useState<string|null>(null);const [error,setError]=useState<string|null>(null);const [uploadProgress,setUploadProgress]=useState<number|null>(null);const load=async()=>{if(!tenantKey)return;setError(null);try{const [o,f,t,c,a]=await Promise.all([getManagedObject(params.objectId,scope),getActiveManagedObjectForm(params.objectId,scope),listTransitionOptions(params.objectId,scope),listComments(params.objectId,scope),listAttachments(params.objectId,scope)]);setItem(o);setForm(f);setOptions(t);setComments(c);setAttachments(a);setValues((o.payload?.currentFormValues as Record<string,unknown>)??{})}catch(e){setError(e instanceof Error?e.message:String(e))}};useEffect(()=>{void load()},[tenantKey,siteKey,params.objectId]);const fields=useMemo(()=>{const raw=form?.rendererDefinition?.fields;return raw&&typeof raw==="object"&&!Array.isArray(raw)?raw as Record<string,Field>:{}},[form]);const action=async(key:string,fn:()=>Promise<unknown>)=>{if(pending)return;setPending(key);setError(null);try{await fn();await load()}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setPending(null)}};const upload=async(file:File)=>{if(!tenantKey||pending)return;setPending("upload");setUploadProgress(0);setError(null);try{const prepared=await prepareMediaUpload(file,{tenantKey,siteKey:siteKey??undefined});const uploaded=await uploadMediaBytes(file,prepared,{tenantKey,siteKey:siteKey??undefined},setUploadProgress);await addAttachment(params.objectId,{assetKey:uploaded.assetKey,fileName:file.name,contentType:file.type||"application/octet-stream",sizeBytes:file.size,downloadUrl:uploaded.deliveryUrl},scope);await load()}catch(e){setError(e instanceof Error?e.message:String(e))}finally{setPending(null);setUploadProgress(null)}};return <PanelShell activeKey="flows" title="Work item" titleFa="مورد کاری" subtitle={params.objectId} subtitleFa={params.objectId}>{error?<div className="operational-banner error" role="alert"><span>{error}</span><button onClick={()=>setError(null)}>×</button></div>:null}{!item||!form?<Skeleton height={600}/>:<div className="work-item-layout"><main><div className="page-action-bar"><div><StatusBadge tone="info">{item.state}</StatusBadge><StatusBadge tone={item.priority==="URGENT"?"danger":"neutral"}>{item.priority??"NORMAL"}</StatusBadge>{item.locked?<span>{locale==="fa"?`قفل‌شده توسط ${item.lockedBy??"—"}`:`Locked by ${item.lockedBy??"—"}`}</span>:null}</div><div><AsyncButton className="secondary-pill" pending={pending==="lock"} onClick={()=>action("lock",()=>setManagedObjectLock(item.id,!item.locked,scope))}>{item.locked?(locale==="fa"?"بازکردن قفل":"Unlock"):(locale==="fa"?"قفل":"Lock")}</AsyncButton></div></div><section className="active-form"><header><div><h2>{form.formKey??(locale==="fa"?"فرم فعال":"Active form")}</h2><p>{form.entityService} · {form.entityKey}</p></div></header>{Object.keys(fields).length?<div className="generated-work-form">{Object.entries(fields).map(([name,field])=><WorkField key={name} name={name} field={field} value={values[name]} onChange={v=>setValues(x=>({...x,[name]:v}))}/>)}</div>:<CodeViewer value={values}/>}<AsyncButton pending={pending==="submit"} onClick={()=>action("submit",()=>submitManagedObjectForm(item.id,{formData:values},scope))}>{locale==="fa"?"ثبت فرم":"Submit form"}</AsyncButton></section><section className="transition-actions"><h2>{locale==="fa"?"اقدام‌های مجاز":"Available transitions"}</h2>{options.length?<div>{options.map(option=><AsyncButton key={option.transitionId} pending={pending===`transition-${option.transitionId}`} className="secondary-pill" onClick={()=>action(`transition-${option.transitionId}`,()=>transitionManagedObject(item.id,{nextState:option.toState,context:{}},scope))}>{option.label}</AsyncButton>)}</div>:<p>{locale==="fa"?"انتقال مجازی نیست.":"No transition is currently allowed."}</p>}</section><section><h2>{locale==="fa"?"تاریخچه":"History"}</h2><CodeViewer value={{audit:item.auditLog,transitions:item.transitionHistory,automation:item.automationBlockRegistry}}/></section></main><aside className="work-collaboration"><section><h2>{locale==="fa"?"مسئول":"Assignment"}</h2><input value={assignee} onChange={e=>setAssignee(e.target.value)} placeholder={item.assignee??"username"}/><AsyncButton pending={pending==="assign"} disabled={!assignee.trim()} onClick={()=>action("assign",()=>assignManagedObject(item.id,assignee,"USER",scope))}>{locale==="fa"?"تخصیص":"Assign"}</AsyncButton></section><section><h2>{locale==="fa"?"نظرها":"Comments"}</h2><div className="comment-list">{comments.map(c=><article key={c.id}><strong>{c.authorUserId??"—"}</strong><p>{c.body}</p><time>{c.createdAt?new Date(c.createdAt).toLocaleString(locale):""}</time></article>)}</div><textarea value={comment} onChange={e=>setComment(e.target.value)} placeholder={locale==="fa"?"نظر بنویسید":"Write a comment"}/><AsyncButton pending={pending==="comment"} disabled={!comment.trim()} onClick={()=>action("comment",async()=>{await addComment(item.id,comment,scope);setComment("")})}>{locale==="fa"?"ارسال نظر":"Add comment"}</AsyncButton></section><section><h2>{locale==="fa"?"پیوست‌ها":"Attachments"}</h2>{attachments.map(a=><article key={a.id}><strong>{a.fileName??a.assetKey}</strong><small>{a.contentType}</small></article>)}<label className="attachment-picker"><span>{pending==="upload"?(locale==="fa"?`بارگذاری ${uploadProgress??0}٪`:`Uploading ${uploadProgress??0}%`):(locale==="fa"?"افزودن فایل":"Add file")}</span><input type="file" disabled={pending==="upload"} onChange={e=>{const f=e.target.files?.[0];if(f)void upload(f)}}/></label></section></aside></div>}</PanelShell>}
function WorkField({name,field,value,onChange}:{name:string;field:Field;value:unknown;onChange:(v:unknown)=>void}){if(field.type==="boolean")return <label className="generated-field"><input type="checkbox" checked={Boolean(value)} onChange={e=>onChange(e.target.checked)}/><span>{field.label??name}</span></label>;if(field.type==="object"||field.type==="list")return <label className="generated-field"><span>{field.label??name}</span><textarea dir="ltr" value={value==null?"":JSON.stringify(value,null,2)} onChange={e=>{try{onChange(JSON.parse(e.target.value))}catch{}}}/></label>;return <label className="generated-field"><span>{field.label??name}{field.required?" *":""}</span><input required={field.required} type={field.type==="number"||field.type==="integer"?"number":field.type==="date"?"date":"text"} value={value==null?"":String(value)} onChange={e=>onChange(field.type==="number"||field.type==="integer"?Number(e.target.value):e.target.value)}/></label>}
